import { SimbolodolarPipe } from './simbolodolar.pipe';

describe('SimbolodolarPipe', () => {
  it('create an instance', () => {
    const pipe = new SimbolodolarPipe();
    expect(pipe).toBeTruthy();
  });
});
